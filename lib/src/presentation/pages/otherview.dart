import 'package:flutter/material.dart';

class Feets extends StatelessWidget {
  const Feets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Alturas de las montañas '),
      ),
    );
  }
}
